# CPP primer plus 5th examples and solutions
1. Only use by myself
2. Include examples and solutions(in "./ch2/practice" etc.)
3. All source codes are tested and can be compiled to run
4. Windows excutable file is ".exe", while Linux is ".out"
5. Compile in ./output, example : g++ ./ch2/excercises/2.cpp -o ./ch2/excercises/output/2.out
